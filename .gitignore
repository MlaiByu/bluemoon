# ===== Python (blog-api) =====
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.venv/
env/
.Python
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# ===== 环境变量 / 敏感配置 =====
.env
.env.local
.env.production
# 保留模板：!.env.example
!.env.example
# 本机路径覆盖脚本（含个人绝对路径），入口见 local.env.bat.example
local.env.bat

# ===== Node.js / Vite (blog-web) =====
node_modules/
dist/
dist-ssr/
*.local
vite.config.js.timestamp-*.mjs
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ===== Redis 发行版（二进制工具，不入库） =====
_tools/redis/

# ===== 用户上传文件（日期布局 + 站点级资产）=====
# 运行时产物，部署环境各自生成。
# 注意：必须写成 static/* 而非 static/ —— 忽略整个目录会让下面的
# .gitkeep 例外失效（Git 不会进入被忽略的目录找例外文件），
# 结果 clone 后 static/ 根本不存在，上传接口直接报错。
blog-api/static/*
!blog-api/static/.gitkeep

# ===== 日志 =====
*.log
logs/

# ===== AI 工具临时文件 =====
.playwright-cli/
.workbuddy/

# ===== 自检 / 验证产物（开发过程临时生成，勿提交） =====
# 截图、验证脚本等一次性调试资源，完成验证后即清理
docs/theme-switch-preview/
scripts/verify/
_tools/verify-*.cjs
_tools/verify-*.mjs
_tools/verify-*.py
shot-*.png
*-screenshot.png

# ===== IDE / 编辑器 =====
.idea/
.vscode/
*.swp
*.swo

# ===== 系统文件 =====
.DS_Store
Thumbs.db
desktop.ini
# Windows 保留名误创建的文件
nul
